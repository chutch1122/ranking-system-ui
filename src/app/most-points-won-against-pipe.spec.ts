import { MostPointsWonAgainstPipe } from './most-points-won-against.pipe';

describe('MostPointsWonAgainstPipe', () => {
  it('create an instance', () => {
    const pipe = new MostPointsWonAgainstPipe();
    expect(pipe).toBeTruthy();
  });
});
